services:
  app:
    build: .
    environment:
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
      - BETTER_AUTH_URL=${BETTER_AUTH_URL}
    volumes:
      - pglite_data:/app/data/pglite
    ports:
      - "3111:3000"
    restart: unless-stopped

volumes:
  pglite_data:
